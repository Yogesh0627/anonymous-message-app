# The react-email / ai stack ships optional peer deps (e.g. Svelte's sswr) that
# conflict harmlessly under npm 7+ strict mode. This lets `npm install` /
# `npm ci` succeed locally, in CI, and on Vercel without a manual flag.
legacy-peer-deps=true
